#include <bits/stdc++.h>
using namespace std;

int main(){
    double A, B, C, MEDIA;
    cout << fixed << setprecision(1);
    cin >> A;
    cin >> B;
    cin >> C;

    MEDIA = (A * 2.0 + B * 3.0 + C * 5.0) /10;
    cout << "MEDIA = " << MEDIA  << endl;
return 0;
}
